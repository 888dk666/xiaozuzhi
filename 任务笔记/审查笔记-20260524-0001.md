# 任务笔记-20260524-0001

## 0.1 查历史+现状

**现状：**
- 涉及文件：`~/projects/晓组织/operations.md`（753 行，5 章）
- 关键约束：晓组织 CLAUDE.md、小小克 subagent-guide.md
- 注意：逐条过操作手册，发现缺口暂不修，查完一起解决

**历史：** 未查到类似任务，本次为首次逐条审查操作手册

## 待处理缺口（查完统一解决）

1. **模型分层不完整** — spawn 约定只覆盖 PM/架构+QA/工程，合规/财务/秘书用什么模型没写 → ✅ 已补全 6 角色（合规/财务=Sonnet）
2. **团队人数矛盾** — 主流程步骤 5 写"最多 8 个" vs 速查卡常见坑写"3-5 个最佳" → ✅ 已修
3. **Spawn prompt 规范没接上** — 引了小小克指南但没概括核心要点 → ✅ 已通过边界分离解决（小小克⇔晓组织分层）
4. **什么时候不 spawn** — 没有边界，PM 自己直接干 vs 派 Agent 什么时候选哪个没写 → ✅ 已加判据（只读/纯思考→PM自己干，写文件/搜索/执行→派Agent）
5. **Isolation mode 没提** — 小小克指南里有 worktree 隔离，spawn 约定里完全没提 → ✅ 已补（代码改动类→worktree，工程配 isolation）
6. **原生质量闸 vs QA 审方案关系模糊** — 写"对应"但 Plan Approval 是 PM 批准再执行，QA 审方案是架构出完才审，时序和主体都不同，没说是二选一还是各做各的 → ✅ 已厘清（Teams Plan Approval 不内置，晓组织用 QA 审方案替代）
7. **团队数量限制过死** — "团队只有一个"堵死了并行建两个独立团队的可能，没给判断标准 → ✅ 裁决：删掉"团队只有一个"，改为"同时活跃上限 5，见 protocol.md §七"（2026-05-25）
8. **Fork 模式没标实验性** — research.md 里标了实验性，这里写"暂不内置"像成熟功能 → ✅ 已统一标注"不内置"
9. **Agent Teams vs 手动 spawn 没定** — 第一章全是手动 Agent 工具调用，spawn 约定又提 Agent Teams Plan Approval，两套机制并存，没说过什么时候用哪个 → ✅ 裁决：.zshrc env var 关掉 + 文档统一"不内置"（2026-05-25）
10. **子 Agent 实际能力没摸排** — 子 Agent 到底有哪些工具、少了什么、实际行为跟 PM 差多少，没实测过 → ✅ 已知差异已标注（Explore/Plan 不读 CLAUDE.md，后台自动拒权限）
11. **各角色权限模式未定** — spawn 时 acceptEdits/auto/bypassPermissions/default/dontAsk/plan 怎么分配，全用 default 还是按角色分 → ✅ 已按角色分配（见 agent 定义文件）
12. **模型指定能不能跑通** — 写了分层但没测过实际生效情况、不同模型对角色 prompt 响应质量差异 → ⏳ 等下一个任务验证
13. **失败处理不完整** — 只写了架构超时和工程卡住，网络断开、API 超时、子 Agent 乱码等底层故障没覆盖 → ✅ 已加基础三条（网络断开→重试2次，API超时→降级Haiku，乱码→换人重跑）
14. **成本基线缺失** — 每条路径写了预估调用次数，但没实测单次 spawn token 消耗，财务估算缺参照
